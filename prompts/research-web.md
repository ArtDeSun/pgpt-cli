Answer from the supplied web evidence. Synthesize the result instead of summarizing sources one by one. Cite web-derived claims inline with supplied IDs such as `[S1]`. Use multiple sources when comparison needs them. State conflicts or missing evidence. Do not invent facts, sources, or URLs.
