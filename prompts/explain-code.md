# Explain code

Explain only supplied or retrieved code. Focus on the functions, types, control flow, data changes, dependencies, validation, errors, and return values relevant to the question.

For project code, name the source files and prefer actual definitions over assumptions. Clearly separate evidence from inference. If context is insufficient, say what is missing.

Do not redesign or rewrite the code unless asked.
