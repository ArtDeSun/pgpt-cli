web_fetch:
  enabled: true
  timeout_seconds: 5

web_search:
  enabled: true
  provider: brave
  processor: best_links
  num_links: 3
  mode_quality: fast
  cached: true
  context_token: 2000

brave:
  api_key: ${PGPT_BRAVE_API_KEY:}
  rate_limit: 1
